[build]
  base = "client"
  command = "npm run build"
  publish = "dist"

# React Router — serve index.html for all routes (direct links & refresh)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
