# Copy to client/.env.local for local dev (optional — Vite proxy handles /api by default)
# VITE_API_URL=https://boba-wicks-api.onrender.com

# On Netlify you do NOT need VITE_API_URL if /api is proxied via netlify.toml.
# Only set VITE_API_URL if you want the browser to call the API host directly.
